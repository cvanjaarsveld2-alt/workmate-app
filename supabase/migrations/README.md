# Migrations

These files are exported from production (`supabase_migrations.schema_migrations`)
so the repo matches the live database. The earliest tables were created before
migrations were tracked, so a brand-new project cannot be built from these
files alone; they record every tracked change since 2026-09-15.

Apply new changes with the Supabase CLI or MCP and re-export, rather than
editing an applied file.
